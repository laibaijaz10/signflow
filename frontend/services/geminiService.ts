
// AI features have been removed from the application.
// This file is kept as a placeholder to prevent import errors in case of cached references, 
// but it performs no operations.

export const generateLegalClause = async (topic: string): Promise<string> => {
    console.warn("AI generation is disabled.");
    return "";
};

export const summarizeDocumentStatus = async (docTitle: string, status: string, signer: string): Promise<string> => {
    return "";
}
